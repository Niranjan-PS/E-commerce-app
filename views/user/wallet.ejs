<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f3f4f6;
        }
        .container {
            max-width: 960px;
        }
        .navbar {
            background-color: #000000;
            padding: 1rem 2rem;
            box-shadow: 0 2px 8px rgba(124, 122, 122, 0.2);
        }
        .navbar-brand {
            color: #fff !important;
            font-weight: 700;
            font-size: 1.8rem;
            letter-spacing: 1px;
            text-shadow: 0 1px 2px rgba(0,0,0,0.2);
        }
        .nav-link {
            color: #e0e0e0 !important;
            font-weight: 500;
            margin: 0 1rem;
            border-radius: 5px;
            transition: color 0.3s, background 0.3s;
        }
        .nav-link:hover, .nav-link.active {
            background-color: rgba(187, 134, 252, 0.1);
            color: #bb86fc !important;
        }
        .navbar-icons {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }
        .navbar-icons a {
            color: #fff;
            font-size: 1.2rem;
            padding: 0.4rem;
            border-radius: 50%;
            transition: all 0.3s ease;
            position: relative;
        }
        .navbar-icons a:hover {
            background-color: rgba(255,255,255,0.1);
            color: #bb86fc;
            transform: translateY(-2px);
        }
        .badge {
            position: absolute;
            top: -8px;
            right: -8px;
            font-size: 0.7rem;
            padding: 0.25rem 0.5rem;
            border: 2px solid #1a1a1a;
            font-weight: 600;
        }
        .dropdown-menu {
            background-color: #ffffff;
            border: none;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            margin-top: 0.5rem;
        }
        .dropdown-item {
            color: #333333;
            transition: all 0.3s;
            padding: 0.5rem 1.5rem;
        }
        .dropdown-item:hover {
            background-color: #f8f9fa;
            color: #6200ea;
        }
        .navbar-toggler {
            border: 2px solid rgba(255,255,255,0.5);
            padding: 0.5rem;
        }
        .navbar-toggler:focus {
            box-shadow: 0 0 0 3px rgba(187, 134, 252, 0.25);
        }
    </style>
</head>
<body class="antialiased flex flex-col min-h-screen">
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/">LUXE SCENTS</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/shop">Shop</a>
                   
                </ul>
                <div class="navbar-icons d-flex align-items-center">
                    <a href="/wishlist" class="text-white me-3 position-relative">
                        <i class="fas fa-heart"></i>
                        <span class="badge bg-danger" id="wishlistCount">0</span>
                    </a>
                    <a href="/cart" class="text-white me-3 position-relative">
                        <i class="fas fa-shopping-cart"></i>
                        <span class="badge bg-danger" id="cartCount">0</span>
                    </a>
                   
                    <div class="dropdown">
                        <a href="#" class="dropdown-toggle text-white" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user-circle"></i>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="/profile">My Profile</a></li>
                            <li><a class="dropdown-item" href="/addresses">My Addresses</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/logout">Logout</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <main class="flex-grow py-8 px-4">
        <div class="container mx-auto">
            <h1 class="text-3xl font-bold text-gray-800 mb-8 text-center"><%= title %></h1>

            <div class="current-balance-section mb-5 max-w-md mx-auto">
                <div style="background: #181824; color: #fff; border-radius: 16px; box-shadow: 0 2px 12px rgba(0,0,0,0.12); padding: 2rem 1.5rem; display: flex; flex-direction: column; align-items: flex-start; gap: 0.5rem;">
                    <div style="display: flex; align-items: center; width: 100%; justify-content: space-between;">
                        <span style="font-size: 1.1rem; font-weight: 600; color: #fff;">Current Balance</span>
                        <i class="fas fa-wallet" style="font-size: 2rem; color: #bb86fc;"></i>
                    </div>
                    <span style="font-size: 2.2rem; font-weight: 700; color: #bb86fc; margin-top: 0.5rem;">
                        ₹<%= 
                            (typeof wallet !== 'undefined' && typeof wallet.balance !== 'undefined')
                                ? wallet.balance.toLocaleString('en-IN', { minimumFractionDigits: 2, maximumFractionDigits: 2 })
                                : (user && typeof user.wallet !== 'undefined')
                                    ? user.wallet.toLocaleString('en-IN', { minimumFractionDigits: 2, maximumFractionDigits: 2 })
                                    : '0.00'
                        %>
                    </span>
                    <span style="font-size: 0.95rem; color: #b0b0b0;">Available for your next purchase</span>
                </div>
            </div>

            <div class="transaction-history-section max-w-md mx-auto" style="background: #181824; color: #fff; border-radius: 16px; box-shadow: 0 2px 12px rgba(0,0,0,0.12); padding: 2rem 1.5rem; margin-bottom: 2rem;">
                <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 1.2rem;">
                    <span style="font-size: 1.1rem; font-weight: 600; color: #fff;">Transaction History</span>
                    <i class="fas fa-list-alt" style="font-size: 1.7rem; color: #bb86fc;"></i>
                </div>
                <% if (wallet.transactions && wallet.transactions.length > 0) { %>
                    <div style="overflow-x:auto;">
                        <table style="width:100%; border-collapse:separate; border-spacing:0 0.5rem;">
                            <thead>
                                <tr style="color:#bb86fc; font-size:0.95rem;">
                                    <th style="text-align:left; padding:0.5rem 0.5rem 0.5rem 0;">Transaction ID</th>
                                    <th style="text-align:left; padding:0.5rem;">Description</th>
                                    <th style="text-align:left; padding:0.5rem;">Amount</th>
                                    <th style="text-align:left; padding:0.5rem;">Date</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% wallet.transactions.forEach(transaction => { %>
                                    <tr style="background:#232336; border-radius:8px;">
                                        <td style="padding:0.5rem 0.5rem 0.5rem 0; color:#fff; font-size:0.97rem; border-radius:8px 0 0 8px;">
                                            <%= transaction.transactionId %>
                                        </td>
                                        <td style="padding:0.5rem; color:#b0b0b0; font-size:0.97rem;">
                                            <%= transaction.description %>
                                        </td>
                                        <td style="padding:0.5rem; color:#4ade80; font-weight:600; font-size:0.97rem;">
                                            +₹<%= transaction.amount.toLocaleString('en-IN', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %>
                                        </td>
                                        <td style="padding:0.5rem; color:#b0b0b0; font-size:0.97rem; border-radius:0 8px 8px 0;">
                                            <%= new Date(transaction.date).toLocaleString('en-IN', { dateStyle: 'medium', timeStyle: 'short' }) %>
                                        </td>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    </div>
                <% } else { %>
                    <div class="text-center py-5">
                        <i class="fas fa-money-bill-transfer text-4xl" style="color:#bb86fc; margin-bottom:1rem;"></i>
                        <p style="color:#b0b0b0; font-size:1.1rem;">No transactions yet.</p>
                        <p style="color:#888; font-size:0.95rem; margin-top:0.5rem;">Your wallet history will appear here once you have transactions.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </main>

    <%# include('partials/footer') %>  <!-- Fixed: Changed comment syntax -->

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Add this function to update counts from API
        function updateCounts() {
            fetch('/api/cart/count')
                .then(response => response.json())
                .then data => {
                    if (data.success) {
                        document.getElementById('cartCount').textContent = data.cartCount;
                    }
                });

            fetch('/api/wishlist/count')
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        document.getElementById('wishlistCount').textContent = data.wishlistCount;
                    }
                });
        }

        document.addEventListener('DOMContentLoaded', updateCounts);
    </script>
</body>
</html>